export enum HTTPErrorCodes {
  BAD_REQUEST = 400,
  NOT_FOUND = 404,
  CONFLICT = 409,
  SERVER_ERROR = 500,
}

export enum PRISMA_ERROR_CODES {
  NOT_FOUND = "P2025",
  FOREIGN_KEY_NOT_FOUND = "P2003",
  CONFLICT = "P2002",
}
